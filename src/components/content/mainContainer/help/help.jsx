import styles from './help.module.sass'

const Help = () => {
    return(
        <div className={styles.profile_container}>
            <div className={styles.profile_info}>Sorry, right now we don't support this page.</div>
        </div>
    )
}

export default Help;